#include <stdio.h>
main(){
	int v1, v2, soma;
	printf("Digite o valor 1 \n");
	scanf("%d", &v1);
	printf("Digite o valor 2 \n");
	scanf("%d", &v2);
	soma = v1 + v2;
	if(soma > 100){
		printf("\n A soma e : %d", soma);
	}
	system("pause");
}
