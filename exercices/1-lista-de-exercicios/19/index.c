#include <stdio.h>
#include <math.h>
main(){
	int a;
	int b;
	int c;
	int soma;
	printf("Valor1 \n ");
	scanf("%d", &a);
	printf("Valor2 \n ");
	scanf("%d", &b);
	printf("Valor3 \n ");
	scanf("%d", &c); 
	printf("A soma dos quadardos e : %d \n", soma = pow(a,2) + pow(b,2) + pow(c,2) );
	system("pause");
}
